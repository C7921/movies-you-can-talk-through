{
  "name": "movies-you-can-talk-through",
  "version": "1.0.0",
  "description": "The ultimate collection of films that don't mind if you skip the plot for the popcorn!",
  "main": "build.js",
  "scripts": {
    "build": "echo 'Netlify deployment - no build step required'",
    "start": "http-server public -p 8080",
    "dev": "npm run build && npm run start",
    "dev:netlify": "netlify dev",
    "deploy:netlify": "netlify deploy --prod"
  },
  "keywords": [
    "movies",
    "entertainment",
    "tmdb"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "netlify-cli": "^20.0.3"
  }
}
